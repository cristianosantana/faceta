# Fase 4 Ask — Implementation Plan

**Goal:** CLI ask com ≤2 LLM calls (OpenAI gpt-5-mini).

### Task 1: client + understand + resolve + narrate + pipeline + CLI
### Task 2: requirements, docs, verificação ponta a ponta (se key no .env)
