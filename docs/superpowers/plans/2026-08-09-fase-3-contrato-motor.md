# Fase 3 Contrato/Motor — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Contrato + motor genérico + CLI; TC01–TC11 contra dado real.

**Files:** `contrato.yaml`, `faceta/query/{maps,errors,contract,periods,engine,__main__,__init__}.py`, `scripts/fase3_tc.py`, docs, `PyYAML` em requirements.

### Task 1: maps + errors + contrato.yaml + completar contract.py
### Task 2: periods (parse B + shift comparação) + engine.consultar
### Task 3: CLI + script TC01–TC11 + docs
